@model IEnumerable<GestionDesAbsencesMigration.Models.EtudiantAbsent>

@{
    ViewBag.Title = "StatistiquesPDF";
}

<div class="imgs">
    <p>
        <img src="~/assets/imgs/logo2.png" alt="logoensas" height="100" width="160" style="margin-right : 630px;" />

        <img src="~/assets/imgs/Universite_Cadi_Ayyad.png" alt="logoensas" height="100" width="80" />
    </p>
</div>
    <br />
    <h2>Liste des Etudiants convoqués au conseil disciplinaire</h2>
    <br />
    <p>

    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nom)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.prenom)
            </th>
            <th>
                Classe
            </th>
            <th>
                Absence
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.nom)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.prenom)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.nomClass)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.absence_count)
                </td>

            </tr>
        }

    </table>
